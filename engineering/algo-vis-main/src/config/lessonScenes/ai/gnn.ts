import type { AiSceneProfileTable } from "./profile.ts";

export const gnnSceneProfiles = {
  10102: {
    kind: "graph",
    entityLabels: [
      "节点 A", "邻居 B", "邻居 C", "两跳节点 D", "A 自环",
      "A 含自环的度数", "B 的新度数", "归一化边 A-B", "聚合器 A",
      "更新后表示 h_A", "两跳覆盖节点数",
    ],
    sampleValues: [1, 2, 1, 0.2, 0, 0, 0, 0, 0, 0, 0],
    stepValues: [1, 3, 0.41, 1.64, 0.84, 4],
    explanations: [
      "给中心节点 A 加自环后，它聚合时也保留自己的值；A 的可用连接数变为 2。",
      "加入自环后重新计数，邻居 B 的度数为 3，归一化必须使用这组新度数。",
      "A-B 边除以两端度数平方根后权重约为 0.41，度数大的邻居不会压倒其他节点。",
      "A 将自己和邻居的加权消息相加，线性变换前的聚合值为 1.64。",
      "线性层与激活把聚合值写成新表示 h_A=0.84，下一层将读取这个结果。",
      "再堆叠一层后 A 能收到两跳节点 D 的信息，当前可覆盖节点数从 3 扩到 4。",
    ],
  },
  10103: {
    kind: "graph",
    entityLabels: [
      "测试准确率", "标准化输入特征", "P2 类别概率", "训练损失", "参数更新范数",
      "最佳验证轮次", "误判节点数", "训练节点集合 V_train", "类别数 C",
      "真实标签 y_vc", "训练节点预测概率 p_vc",
    ],
    sampleValues: [0, 0, 0, 1, 0, 0, 0, ["P0", "P3"], 3, 1, 0.72],
    stepValues: [1, 0.72, 0.33, 0.06, 12, 0.81, 2],
    explanations: [
      "六个论文节点的词袋特征先按同一规则归一化，当前节点尺度被压到 1。",
      "两层 GCN 聚合引用邻居后，P2 对最高类别给出概率 0.72。",
      "训练掩码只选 P0、P3；当前真实类概率为 0.72，所以单样本交叉熵 -log(0.72)=0.33。",
      "损失反传到共享参数，当前更新范数为 0.06，验证和测试标签仍未参与。",
      "验证节点在第 12 轮表现最好，因此保存这一轮而非按测试集挑模型。",
      "冻结所选模型后，测试节点集合准确率为 0.81，得到一次独立评估。",
      "最后定位 2 个误判论文及其引用邻居，混淆来源由具体节点而非总分揭示。",
    ],
  },
  10104: {
    kind: "graph",
    entityLabels: ["采样/缓存节点数", "目标 A 的状态", "一跳样本数", "二跳样本数", "聚合表示 A", "拼接表示维度"],
    sampleValues: [0, "未选中", 0, 0, 0, 1],
    stepValues: [1, 2, 4, 0.62, 7, 6, 0.74],
    explanations: [
      "从目标节点 A 开始建立计算依赖；只有它的最终嵌入需要在本批输出。",
      "第一层 fan-out=2，只采 B、C 两个邻居，避免读取 A 的完整高阶邻域。",
      "再从 B、C 各采两个外层邻居，最外层一共得到 4 个消息来源。",
      "计算按依赖反向，从 D/E 向内汇总，A 接收到的邻居摘要为 0.62。",
      "把 A 自身特征与邻居摘要拼接，当前表示维度由 4 扩为 7 个示意单元。",
      "去重后采样子图含 6 个节点，这个数直接决定本批主要显存开销。",
      "推理时缓存邻居中间表示，目标 A 最终嵌入摘要为 0.74。",
    ],
  },
  10105: {
    kind: "graph",
    entityLabels: [
      "中心节点 A", "投影后的邻居 B", "边 A-B 分数", "边 A-B 权重",
      "头 1 聚合", "多头输出 A", "投影后的邻居 C", "边 A-C 分数", "边 A-C 权重",
    ],
    sampleValues: [0.5, 0, 0, 0, 0, 0, 0, 0, 0],
    stepValues: [0.64, 1.3, 0.79, 0.71, 1.18],
    explanations: [
      "共享矩阵先投影 A 与邻居特征，B 的投影值从 0.8 变为 0.64。",
      "注意力网络比较 A 和 B 的投影，在边 A-B 上得到未归一化分数 1.3。",
      "只在 A 的邻域 B、C 内做 softmax，A-B 的归一化权重成为 0.79。",
      "头 1 用各边权重汇总邻居，B 的较高权重使聚合结果达到 0.71。",
      "两个头可拼接保留不同关系，也可平均；当前拼接输出摘要为 1.18。",
    ],
  },
  10106: {
    kind: "graph",
    entityLabels: ["编码器更新范数", "训练边数", "负边数量", "节点嵌入 z_A", "边分数 A-B", "重建损失", "ROC-AUC", "留出边数"],
    sampleValues: [0, 100, 0, 0, 0, 0, 0, 0],
    stepValues: [80, 20, 0.61, 0.83, 0.42, 0.07, 0.91],
    explanations: [
      "先从 100 条真实边中只保留 80 条训练边，其余 20 条完全留出评估。",
      "再采 20 条图中不存在的 A-D 类负边，模型才能同时学习连边与不连边。",
      "GNN 只看训练图，把 A 的邻域压成嵌入摘要 z_A=0.61。",
      "A、B 嵌入内积经 sigmoid 后得到连边概率 0.83，分数来自节点表示。",
      "正负边共同计算二元重建损失，当前平均值为 0.42。",
      "损失把梯度 0.07 传回编码器，留出边仍没有泄漏进消息传递。",
      "在留出正边和负边上按分数排序，最终 ROC-AUC 为 0.91。",
    ],
  },
  10107: {
    kind: "graph",
    entityLabels: ["图分类输出", "节点嵌入 Z", "节点到簇 1 权重", "超节点簇 1", "超边强度", "粗粒度节点数", "对照准确率差"],
    sampleValues: [0, 0, 0, 0, 0, 4, 0],
    stepValues: [0.72, 0.8, 1.34, 0.57, 2, 0.88, 0.06],
    explanations: [
      "嵌入 GNN 先为节点 A 生成内容表示 0.72，告诉池化层节点携带什么信息。",
      "分配 GNN 同时给 A 到簇 1 的软权重 0.8，一个节点可以部分属于多个簇。",
      "按分配权重汇总节点后，超节点簇 1 的特征摘要变为 1.34。",
      "用 S 转置、A、S 相乘重建簇间连接，当前超边强度为 0.57。",
      "六节点图被压成 2 个超节点，后续层在更小的图上继续传播。",
      "读出两个超节点得到图级分类置信度 0.88，而非再输出单节点标签。",
      "与其他池化器对照时当前准确率差为 0.06，需同时检查压缩率再选择。",
    ],
  },
  10108: {
    kind: "graph",
    entityLabels: ["事件队列", "当前事件时刻", "发送节点 U 旧记忆", "历史事件 U-W", "当前预测边", "节点 V 新记忆"],
    sampleValues: [4, 0.3, 0.4, 0.1, 0, 0.4],
    stepValues: [1, 0.3, 0.18, 0.76, 0.63],
    explanations: [
      "四个交互先按时间排序，队首事件时刻为 1，模型不会越过它偷看未来。",
      "处理 U 到 V 前先读取两端旧记忆，U 当前记忆摘要为 0.3。",
      "查询时只聚合早于当前时刻的 U-W 事件，时间衰减后的消息为 0.18。",
      "在提交当前事件前，用旧记忆预测 U-V 发生概率 0.76，避免标签泄漏。",
      "预测完成后才把事件写入 V，记忆从 0.4 更新为 0.63，供后续事件使用。",
    ],
  },
  10109: {
    kind: "graph",
    entityLabels: ["用户 U", "有效关系边数", "关系消息权重", "用户嵌入", "ANN 物品数", "Recall@5"],
    sampleValues: [1, 2, 3, 1, 0, 0],
    stepValues: [3, 0.65, 0.72, 1000, 0.8],
    explanations: [
      "schema 校验确认 U 到 A/B 的三条交互边方向和类型一致，错误关系不会进入聚合。",
      "沿用户-物品-类别路径聚合后，物品 A 给 U 的归一化消息权重为 0.65。",
      "消息传递生成用户和物品向量，当前用户 U 的嵌入摘要为 0.72。",
      "把 1000 个物品向量写入 ANN 索引，在线请求无需逐个做全量内积。",
      "检索 top-5 后命中 4 个相关物品，Recall@5=0.8，直接衡量召回覆盖。",
    ],
  },
  10110: {
    kind: "graph",
    entityLabels: ["问题节点 Q", "规范三元组数", "关系编码", "检索路径分数", "证据路径数", "答案支持度"],
    sampleValues: [1, 0, 0, 0, 0, 0],
    stepValues: [3, 0.67, 0.82, 2, 0.94],
    explanations: [
      "文本先规范成 Ada-wrote-P 等 3 个三元组，同一实体别名被合并为一个节点。",
      "关系 wrote 作为注意力偏置参与编码，使 Ada 与论文 P 的边表示达到 0.67。",
      "问题节点先命中 Ada，再沿 wrote 边找到 P，整条证据路径相关度为 0.82。",
      "整理上下文时只保留 2 条可引用路径，生成器看到的是来源明确的证据。",
      "答案中的主张回查到证据路径后支持度为 0.94；无依据主张不会被当作已核验。",
    ],
  },
} satisfies AiSceneProfileTable;
