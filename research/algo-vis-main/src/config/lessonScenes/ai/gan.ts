import type { AiSceneProfileTable } from "./profile.ts";

export const ganSceneProfiles = {
  10119: {
    kind: "distribution",
    entityLabels: ["输入噪声 z", "小特征图", "生成图清晰度", "判别压缩响应", "对抗更新平衡"],
    sampleValues: [0.42, 0, 0, 0, 0],
    stepValues: [0.18, 0.67, 0.74, 0.51],
    explanations: [
      "随机向量 z 经全连接层重排成小特征图，当前有效信号为 0.18。",
      "转置卷积逐级增加空间尺寸，生成图的结构清晰度由 0 提升到 0.67。",
      "判别器反向用普通卷积压缩图像，当前真假响应为 0.74。",
      "先更新判别器再更新生成器后，两方损失平衡度为 0.51，任何一方都未完全压制另一方。",
    ],
  },
  10120: {
    kind: "distribution",
    entityLabels: ["崩溃风险", "噪声样本覆盖", "特征可分性", "类别覆盖率", "训练步对齐趋势"],
    sampleValues: [0, 0, 0, 0, 0],
    stepValues: [0.9, 0.31, 0.22, 0.18, 0.86],
    explanations: [
      "固定模型后采多组不同噪声，输入覆盖率为 0.9，排除采样本身过窄。",
      "生成样本映射到感知特征后，可分性只有 0.31，许多输出已聚在一起。",
      "两两距离和类别覆盖共同给出多样性 0.22，低值直接显示输出趋同。",
      "把指标对齐训练步后，覆盖在当前区间跌到 0.18，可定位崩溃开始的时刻。",
      "同期判别器过强使综合崩溃风险达到 0.86，因此原因不只是随机波动。",
    ],
  },
  10121: {
    kind: "distribution",
    entityLabels: ["生成器学习信号", "真假样本间距", "插值位置 x-hat", "critic 梯度超额", "梯度惩罚"],
    sampleValues: [0, 0, 0, 0, 0],
    stepValues: [0.76, 0.4, 0.28, 0.08, 0.63],
    explanations: [
      "真实样本与生成样本在当前特征轴相距 0.76，为插值线段提供两端。",
      "均匀随机系数把插值点放在线段的 0.4 处，而不是只在真实数据点检查。",
      "critic 对插值输入的梯度范数为 1.28，因此超过目标 1 的部分为 0.28。",
      "平方惩罚把这次偏差转成 0.08 的损失，梯度会把 critic 斜率拉回 1。",
      "多次 critic 更新后再更新生成器，当前可用学习信号为 0.63。",
    ],
  },
  10122: {
    kind: "distribution",
    entityLabels: ["最终质量/多样性平衡", "映射潜变量 w", "逐层混合风格", "截断后风格", "生成图质量"],
    sampleValues: [0, 0, 0, 0, 0],
    stepValues: [0.62, 0.74, 0.51, 0.87, 0.79],
    explanations: [
      "两份噪声分别经过映射网络，当前得到的 w 风格距离为 0.62。",
      "在切换层前用来源 A、之后用来源 B，逐层风格混合强度为 0.74。",
      "另一支固定单一来源并用 psi 拉向平均风格，截断后多样性保留为 0.51。",
      "截断风格注入各合成层后，生成图的感知质量达到 0.87。",
      "联合扫描切换层和 psi 后，选中配置的质量/多样性平衡分为 0.79。",
    ],
  },
  10123: {
    kind: "distribution",
    entityLabels: ["加权训练目标", "X 到 Y 翻译质量", "X 循环重建", "Y 反向循环", "局部真实度"],
    sampleValues: [0, 0, 0, 0, 0],
    stepValues: [0.68, 0.81, 0.77, 0.73, 0.34],
    explanations: [
      "生成器 G 把 X 样本翻译到 Y 域，当前风格匹配质量为 0.68。",
      "F 再把译图送回 X，循环重建相似度达到 0.81，内容因此受到约束。",
      "Y 样本也走 F 再走 G 的反向循环，重建相似度为 0.77。",
      "PatchGAN 在局部块上判断真伪，当前译图的局部真实度为 0.73。",
      "对抗、循环和身份损失加权后总目标归一化为 0.34，并共同更新两组生成器。",
    ],
  },
  10124: {
    kind: "distribution",
    entityLabels: ["生成器回传梯度", "增强参数强度", "增强真实样本", "增强生成样本", "判别差距"],
    sampleValues: [0, 0, 0, 0, 0],
    stepValues: [0.2, 0.84, 0.66, 0.18, 0.37],
    explanations: [
      "本批只采一次颜色和位移参数，合成增强强度为 0.2。",
      "真实图应用这组可微变换后仍保留 0.84 的语义内容。",
      "生成图必须应用同一分布的变换，增强后内容保留度为 0.66。",
      "判别器看到的两类都已增强，当前真假分数差仅为 0.18，不能靠增强痕迹作弊。",
      "梯度穿过增强算子回到生成器，当前有效回传强度为 0.37。",
    ],
  },
  10125: {
    kind: "distribution",
    entityLabels: ["选定工作点", "类别条件强度", "潜变量接受率", "生成批次质量", "质量/覆盖指标"],
    sampleValues: [0, 0, 0, 0, 0],
    stepValues: [0.9, 0.68, 0.86, 0.72, 0.79],
    explanations: [
      "先固定类别嵌入，条件匹配强度为 0.9，随后所有样本都生成同一类别。",
      "阈值 tau 截去尾部潜变量后，重采样接受率为 0.68，分布变窄。",
      "用保留下来的 z 生成一批图像，感知质量提升到 0.86。",
      "更窄分布会减少覆盖，当前精度/召回综合指标为 0.72。",
      "扫描多个 tau 后选择综合分 0.79 的点，而不是只追求最清晰的少数样本。",
    ],
  },
  10126: {
    kind: "distribution",
    entityLabels: ["最终编辑质量", "反演潜变量 w0", "候选语义匹配", "联合损失", "更新后潜变量"],
    sampleValues: [0, 0, 0, 0, 0],
    stepValues: [0.71, 0.58, 0.42, 0.76, 0.88],
    explanations: [
      "先把原图反演到 w0，重建身份相似度为 0.71，后续编辑从这里出发。",
      "生成当前候选后，它与文字提示的语义匹配度只有 0.58。",
      "mask 内使用文本目标、mask 外使用保持项，联合损失为 0.42。",
      "损失反传使潜变量沿语义方向移动，更新后的约束满足度为 0.76。",
      "最终只用同一 mask 合成编辑区，身份保持与文本匹配的综合质量达到 0.88。",
    ],
  },
} satisfies AiSceneProfileTable;
