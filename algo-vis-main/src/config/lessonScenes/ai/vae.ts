import type { AiSceneProfileTable } from "./profile.ts";

export const vaeSceneProfiles = {
  10127: {
    kind: "distribution",
    entityLabels: [
      "可回传梯度", "后验均值 mu", "后验 log-variance", "标准差 sigma",
      "标准噪声 epsilon", "潜样本 z", "编码器输入 x", "标准高斯 N(0,I)",
    ],
    sampleValues: [0, 0, 0, 0, 0, 0, 0.8, 1],
    stepValues: [0.4, 0.6, 0.25, 0.55, 0.72],
    explanations: [
      "编码器为当前输入同时预测均值 mu=0.4 与 log-variance=log(0.36)，两个量分开显示并可独立检查。",
      "标准差由 exp(0.5xlogvar) 得到：exp(0.5xlog(0.36))=0.6，少了 0.5 会把尺度算大。",
      "从固定标准高斯源取得本次 epsilon=0.25，随机性与网络参数由此分离。",
      "缩放平移得到 z=0.4+0.6x0.25=0.55，这一步对均值和标准差都是确定性运算。",
      "损失沿 z 的确定性路径返回编码器，当前归一化梯度可用度为 0.72。",
    ],
  },
  10128: {
    kind: "distribution",
    entityLabels: ["潜变量属性可控度", "后验信息量", "重构质量", "KL 代价", "beta 加权目标"],
    sampleValues: [0, 0, 0, 0, 0],
    stepValues: [0.4, 0.82, 0.18, 0.36, 0.71],
    explanations: [
      "编码器给出潜分布，当前相对先验保留的信息量为 0.4。",
      "从后验采样后，解码器对输入的重构质量达到 0.82。",
      "重构误差和 KL 分开统计，当前归一化 KL 代价为 0.18。",
      "beta=2 时 KL 的加权贡献变为 0.36，瓶颈因而比普通 VAE 更强。",
      "逐维改变 z 后，单一属性的可控度为 0.71；它需与重构质量一起判断解耦。",
    ],
  },
  10129: {
    kind: "distribution",
    entityLabels: ["推理生成质量", "训练后验集中度", "训练重构质量", "训练总损失", "条件先验样本", "同条件多样性诊断"],
    sampleValues: [0, 0, 0, 0, 0, 0],
    stepValues: [0.68, 0.82, 0.31, 0.55, 0.86, 0.77],
    explanations: [
      "训练时编码器同时读取样本 x 和条件 c，当前后验集中度为 0.68。",
      "从后验采样并连同 c 交给解码器，训练重构质量达到 0.82。",
      "重构项与后验/条件先验 KL 相加，当前归一化训练损失为 0.31。",
      "推理没有真实 x，只能从 p(z|c) 采样；当前条件先验样本强度为 0.55。",
      "把 z 和 c 直接送入解码器后，生成样本的条件一致质量达到 0.86。",
      "固定 c 重采样 z 后，多样性诊断为 0.77，说明模型没有只生成一个模板。",
    ],
  },
  10130: {
    kind: "distribution",
    entityLabels: ["直通梯度", "连续编码 z_e", "最近码字相似度", "重构质量", "三项损失", "码本命中健康度"],
    sampleValues: [0, 0, 0, 0, 0, 0],
    stepValues: [0.55, 0.91, 0.8, 0.27, 0.63, 0.83],
    explanations: [
      "编码器先输出连续特征 z_e，当前归一化幅度为 0.55。",
      "与码本逐项比较后，最近码字相似度为 0.91，并以其索引替换连续表示。",
      "选中的 z_q 进入解码器，当前重构质量达到 0.8。",
      "重构、码本和承诺损失合计为 0.27，停止梯度决定每项更新哪一侧。",
      "直通估计把解码梯度复制给编码器，当前可用梯度强度为 0.63。",
      "最后按梯度或 EMA 更新码本，命中分布健康度升到 0.83。",
    ],
  },
  10131: {
    kind: "distribution",
    entityLabels: ["K 增大后的收益", "样本数占预算比", "log-weight 稳定度", "有效样本率", "批次下界"],
    sampleValues: [0, 0, 0, 0, 0],
    stepValues: [0.25, 0.88, 0.61, 0.73, 0.12],
    explanations: [
      "当前取 K=4，相对最大预算 16 的占比为 0.25，四个 z 都来自同一后验。",
      "逐样本计算 log p-log q 后，数值稳定度为 0.88，尚未执行危险的直接指数求和。",
      "logsumexp 归一化权重后，有效样本率为 0.61，说明并非只有一个样本独占权重。",
      "对批次取平均得到归一化下界 0.73；K=1 时应退化为普通 ELBO。",
      "K 从 1 增到 4 带来的界改善为 0.12，同时需要四倍潜样本计算。",
    ],
  },
  10132: {
    kind: "distribution",
    entityLabels: ["告警与漂移指标", "正常分数拟合度", "新窗口重构误差", "组合异常分数", "验证阈值 tau"],
    sampleValues: [0, 0, 0, 0, 0],
    stepValues: [0.9, 0.48, 0.67, 0.6, 1],
    explanations: [
      "只用正常窗口训练后，正常分数分布拟合度为 0.9，建立了可比较的基线。",
      "新窗口经编码和重构后，标准化重构误差为 0.48。",
      "重构误差与 KL 按权重相加，组合异常分数升到 0.67。",
      "验证集按目标 precision/recall 选出阈值 0.6，而不是直接复制训练损失。",
      "因为 0.67 大于 0.6，本窗口触发告警值 1；后续分布漂移也由同一指标监控。",
    ],
  },
  10133: {
    kind: "distribution",
    entityLabels: ["最终重构质量", "底层输入特征", "顶层全局潜变量", "条件先验", "融合后底层潜变量"],
    sampleValues: [0, 0, 0, 0, 0],
    stepValues: [0.62, 0.71, 0.66, 0.79, 0.88],
    explanations: [
      "自底向上编码输入后，靠近数据的局部特征强度为 0.62。",
      "顶层从后验采样全局结构 z_L，当前信息量为 0.71。",
      "全局变量向下生成下一层条件先验，预测强度为 0.66。",
      "底层后验与条件先验融合并采样，局部潜变量有效信息量升到 0.79。",
      "解码器从 z_1 重构数据，质量达到 0.88；各层 KL 非零说明没有被绕过。",
    ],
  },
  10134: {
    kind: "distribution",
    entityLabels: ["rollout 校正后表现", "观测后验 z_t", "策略动作 a_t", "动力学先验", "KL/价值监督", "高维观测 o_t"],
    sampleValues: [0, 0, 0, 0, 0, 0.9],
    stepValues: [0.7, 0.35, 0.64, 0.28, 0.82],
    explanations: [
      "编码器结合观测和历史得到潜状态后验，当前可预测信息量为 0.7。",
      "策略只读取 z_t 并选择动作，当前动作强度为 0.35，背景像素无需直接进入策略。",
      "动力学模型根据 z_t 和动作预测下一先验，和真实后验的匹配度为 0.64。",
      "KL、重构与价值目标共同监督表示，当前综合损失为 0.28。",
      "在潜模型 rollout 后用真实转移校正，策略的归一化控制表现达到 0.82。",
    ],
  },
} satisfies AiSceneProfileTable;
