import type { AiSceneProfileTable } from "./profile.ts";

export const rnnSceneProfiles = {
  10082: {
    kind: "sequence",
    entityLabels: [
      "新隐藏状态 h_t", "当前输入 x_t", "上一状态 h_(t-1)", "线性和 a_t",
      "序列输入样本", "历史状态缓存",
    ],
    sampleValues: [0, 0, 0, 0, 0.6, 0.2],
    stepValues: [0.6, 0.2, 0.5, 0.46],
    explanations: [
      "时间步 t 先读取输入 0.6；这个值提供当前 token 的新信息。",
      "同时取出上一时刻留下的隐藏状态 0.2，它把过去压缩后带到当前步。",
      "两路分别乘权重并加偏置，得到预激活值 0.5，而不是直接覆盖旧状态。",
      "tanh 把 0.5 压到约 0.46，新状态随后传给下一个时间步。",
    ],
  },
  10083: {
    kind: "sequence",
    entityLabels: [
      "堆叠输出个数", "初始状态 h0", "当前 token x_t", "当前隐藏状态 h_t",
      "初始零状态", "输入 token 样本", "输入序列长度 T",
    ],
    sampleValues: [0, "未初始化", 0, 0, 0, 3, 3],
    stepValues: [0, 3, 0.54, 3],
    explanations: [
      "序列开始时把隐藏状态设为 0，因此第一个 token 不会混入虚构的历史。",
      "指针按时间读取三个 token，当前 token 的值进入同一个共享 RNN cell。",
      "每步的新状态依赖当前输入和前一步状态；第三步得到的隐藏值为 0.54。",
      "三个时间步各留下一个输出，按原时间顺序堆叠成长度 3 的结果序列。",
    ],
  },
  10084: {
    kind: "sequence",
    entityLabels: ["早期梯度范数", "末端梯度", "经过 tanh 导数", "经过循环权重"],
    sampleValues: [0, 0, 0, 0],
    stepValues: [1, 0.42, 0.25, 0.03],
    explanations: [
      "反向传播从末端损失开始，末端隐藏状态接收到范数 1 的梯度。",
      "饱和区的 tanh 导数只有 0.42，第一次数乘就丢掉了超过一半的信号。",
      "再乘循环权重后梯度降到 0.25；每倒退一步都会重复相似的缩放。",
      "回到较早时间步时范数只剩 0.03，这就是远距离依赖难以学习的直接原因。",
    ],
  },
  10085: {
    kind: "sequence",
    entityLabels: ["隐藏输出 h_t", "门值摘要", "候选记忆 g_t", "新细胞状态 c_t", "旧细胞状态 c_(t-1)"],
    sampleValues: [0, 0, 0, 0, 0.3],
    stepValues: [0.73, 0.6, 0.44, 0.31],
    explanations: [
      "输入和旧状态先产生门值；例如遗忘门 0.73 决定旧记忆保留多少。",
      "候选分支提出新内容 0.6，但它还要经过输入门筛选才能写入。",
      "旧记忆的保留项与新候选的写入项相加，使细胞状态更新为 0.44。",
      "输出门再筛选 tanh(c_t)，最终暴露的隐藏状态约为 0.31。",
    ],
  },
  10086: {
    kind: "sequence",
    entityLabels: [
      "验证准确率", "输入维度 d", "隐藏宽度 h", "LSTM 门数",
      "GRU 门数", "LSTM 参数量", "GRU 参数量", "公平比较维度",
      "LSTM 门集合", "GRU 门集合",
    ],
    sampleValues: [
      0, 0, 0, 0, 0, 0, 0, 16,
      ["输入门", "遗忘门", "输出门", "候选记忆"],
      ["更新门", "重置门", "候选状态"],
    ],
    stepValues: [16, 4, 2112, 0.86],
    explanations: [
      "先把 LSTM 和 GRU 的输入、隐藏宽度都设为 16，避免尺寸差异干扰比较。",
      "LSTM 有四组门控线性层，而 GRU 有三组，所以前者参数通常更多。",
      "代入 d=h=16 后，LSTM 有 4x528=2112 个参数，GRU 有 3x528=1584 个参数。",
      "在同一数据和预算下比较后，当前较优模型验证准确率为 0.86，选择依据来自指标而非门数。",
    ],
  },
  10087: {
    kind: "sequence",
    entityLabels: ["双向结果增益", "前向状态", "逆序后向状态", "复原后的后向状态", "合并表示"],
    sampleValues: [0, 0, 0, 0, 0],
    stepValues: [0.64, 0.51, 0.51, 1.15, 0.18],
    explanations: [
      "前向 RNN 从左到右处理，当前位置得到包含左侧上下文的状态 0.64。",
      "后向 RNN 从右到左处理，同一位置因此能读到右侧上下文并得到 0.51。",
      "后向计算完成后要翻回原时间顺序，0.51 才能与正确的 token 对齐。",
      "拼接时两方向信息并列保留；用简化求和表示时当前合并强度为 1.15。",
      "相对单向基线，双向上下文在当前离线任务上带来 0.18 的指标增益。",
    ],
  },
  10088: {
    kind: "sequence",
    entityLabels: ["终止 token EOS", "编码上下文 c", "起始符 BOS", "当前预测 token", "解码初始状态"],
    sampleValues: [0, 0, 1, 0, 0],
    stepValues: [0.72, 0.72, 42, 2],
    explanations: [
      "编码器读完整个源序列，把主要信息压进最终上下文向量，摘要值为 0.72。",
      "编码器上下文 0.72 被复制到解码器初始状态，源句信息由此跨过编码/解码边界。",
      "解码器以 BOS=1 启动，并结合初始状态选出 token 42；它会成为下一步输入。",
      "当模型输出 EOS=2 时停止，最终序列包含已生成的内容而不再无限展开。",
    ],
  },
  10089: {
    kind: "sequence",
    entityLabels: ["解码前缀分数", "含 blank 的状态数", "允许的转移边", "路径总概率", "CTC 损失"],
    sampleValues: [0, 0, 0, 0, 0],
    stepValues: [5, 8, 0.18, 1.71, 0.63],
    explanations: [
      "目标 AB 插入 blank 后成为 _A_B_，动态规划因此有 5 个合法状态。",
      "每个状态可停留、前进，字符不重复时还能跨过 blank，当前图共有 8 条允许边。",
      "动态规划把所有能折叠成 AB 的路径概率相加，当前总概率为 0.18。",
      "损失取负对数，所以 -log(0.18) 约为 1.71；正确路径总概率越高，损失越低。",
      "前缀束搜索合并会折叠成同一文本的路径，最终保留 AB 的累计分数 0.63。",
    ],
  },
  10090: {
    kind: "sequence",
    entityLabels: ["预测 token 编号", "匹配分数 e_ti", "注意力峰值 alpha", "上下文 c_t"],
    sampleValues: [0, 0, 0, 0],
    stepValues: [0.8, 0.55, 0.62, 7],
    explanations: [
      "解码状态分别与各编码状态比较，当前最相关位置的未归一化分数为 0.8。",
      "softmax 把所有位置分数变成总和为 1 的权重，最高权重为 0.55。",
      "编码状态按权重求和后得到上下文摘要 0.62，重要位置贡献更大。",
      "解码器把上下文与自身状态合并，当前选择词表中的 token 7。",
    ],
  },
  10091: {
    kind: "sequence",
    entityLabels: ["验证损失", "有效 token 数", "归一化激活", "屏蔽后训练损失", "梯度范数"],
    sampleValues: [0, 0, 0, 0, 0],
    stepValues: [4, 0.81, 0.37, 0.24, 0.34],
    explanations: [
      "长度为 4 的样本只标记前四个 token 有效，padding 位置的 mask 为 0。",
      "训练时 dropout 随机丢弃部分连接，LayerNorm 再把当前位置的激活稳定到 0.81。",
      "逐位置损失乘 mask 后，padding 不再贡献梯度，有效位置平均损失为 0.37。",
      "反向传播只穿过有效 token，当前裁剪后的梯度范数为 0.24。",
      "验证时关闭 dropout，得到稳定损失 0.34，训练和推理模式不会混用。",
    ],
  },
} satisfies AiSceneProfileTable;
