import type { AiSceneProfileTable } from "./profile.ts";

export const transformerSceneProfiles = {
  10092: {
    kind: "matrix",
    entityLabels: [
      "输入 token 数", "嵌入后 token", "编码器上下文", "解码器状态", "下一个 token",
      "位置向量", "掩码注意力", "交叉注意力", "FFN 残差",
    ],
    sampleValues: [3, 0, 0, 0, 0, 3, 0, 0, 0],
    stepValues: [3, 0.68, 0.74, 17],
    explanations: [
      "三个 token 分别加上位置向量，因此相同词出现在不同位置也会得到不同表示。",
      "编码器自注意力让每个 token 汇入全句信息，当前上下文强度由 0 提升到 0.68。",
      "解码器先看已生成前缀，再用交叉注意力从编码结果取信息，状态更新为 0.74。",
      "输出投影把解码状态变成词表分数，当前最高分对应 token 17。",
    ],
  },
  10093: {
    kind: "matrix",
    entityLabels: [
      "token 位置 p", "编码方案", "频率尺度", "sin/cos 分量", "可学习表项",
      "相加后的嵌入", "注意力位移敏感度", "模型宽度", "token 嵌入",
      "维度对索引 i", "正弦维度 j=2i", "余弦维度 j=2i+1",
    ],
    sampleValues: [2, 0, 0, [0, 0], 0, 0, 0, 8, 0.4, 1, 2, 3],
    stepValues: [1, 0.1, [0.2, 0.98], 0.35, 0.75, 0.22],
    explanations: [
      "先选择固定正弦编码作为当前分支；这个选择决定位置值是否参与学习。",
      "第 2 个位置在低频维度使用尺度 0.1，因此相邻位置变化平缓。",
      "p=2、频率尺度 0.1 时，偶数维写 sin(0.2)=0.20，奇数维写 cos(0.2)=0.98。",
      "可学习方案不计算三角函数，而是直接读取位置 2 的表项 0.35。",
      "位置向量与 token 嵌入 0.4 逐元素相加，当前维度变成 0.75。",
      "比较注意力图时，位置编码使位移后的权重差达到 0.22，说明模型已感知顺序。",
    ],
  },
  10094: {
    kind: "matrix",
    entityLabels: [
      "输入表示 H", "Q/K/V 投影", "缩放相似度", "mask 后注意力权重", "多头上下文",
      "Q 头 1", "K 头 1", "V 头 1", "拼接输出",
    ],
    sampleValues: [2, 0, 0, 0, 0, 1, 1, 3, 0],
    stepValues: [3, 0.71, 0.67, 2.34],
    explanations: [
      "同一输入经三组权重得到 Q、K、V；两 token 两头的示例一共产生三类投影。",
      "Q 与 K 点积后除以 sqrt(d_k)，把过大的分数缩到 0.71 以稳定 softmax。",
      "未来位置先被 mask 排除，剩余分数归一化后当前最大权重为 0.67。",
      "各头按权重汇总 V，再把两个头拼接，当前位置输出示例为 2.34。",
    ],
  },
  10095: {
    kind: "matrix",
    entityLabels: [
      "位置 token 1", "线性层输入", "扩展维度 d_ff", "激活后特征", "回投影 d_model",
      "位置 token 2", "权重 W1", "权重 W2", "偏置项",
    ],
    sampleValues: [0.5, 0, 0, 0, 0, -0.2, 2, 1, 0.1],
    stepValues: [0.5, 4, 1.2, 0.7],
    explanations: [
      "每个位置独立把自己的 0.5 向量送入同一线性层，位置之间此处不通信。",
      "第一层把表示从 d_model=2 扩到 d_ff=4，为非线性组合提供更多通道。",
      "GELU 将负响应压低并保留正响应，当前激活摘要从线性值变为 1.2。",
      "第二层把四维特征投回两维，当前 token 得到 0.7，随后才能与残差相加。",
    ],
  },
  10096: {
    kind: "matrix",
    entityLabels: [
      "原始激活 x", "归一化方案", "Pre-LN 输入", "Pre-LN 残差和", "Post-LN 残差和",
      "Post-LN 输出", "梯度范数比", "均值 mu", "方差 sigma2", "稳定项 epsilon",
      "缩放系数 gamma", "平移参数 beta", "隐藏维度 d", "Pre-LN 子层输出",
      "Post-LN 子层输出",
    ],
    sampleValues: [2, 0, 0, 0, 0, 0, 0, 1, 4, 0.01, 1, 0, 4, 0.4, 1.1],
    stepValues: [1, 0.499, 2.4, 3.1, 1.049, 0.93],
    explanations: [
      "先明确比较 Pre-LN 与 Post-LN 两条路径；两者使用相同原始激活 2。",
      "取 gamma=1、beta=0、epsilon=0.01，(2-1)/sqrt(4.01)=0.499，再送入子层。",
      "Pre-LN 子层输出 0.4 与未经归一化的 x=2 相加，残差和为 2.4。",
      "Post-LN 子层先输出 1.1，再与 x=2 相加，归一化前残差和为 3.1。",
      "最后按同一组统计量归一化：(3.1-1)/sqrt(4.01)=1.049。",
      "两路梯度范数比为 0.93；直接量化它能判断深层堆叠是否稳定。",
    ],
  },
  10097: {
    kind: "matrix",
    entityLabels: [
      "输入嵌入 H0", "第 1 层输入", "自注意力上下文", "FFN 更新", "顶层表示 HL",
      "残差支路", "第 2 层", "层数 L", "token 数",
    ],
    sampleValues: [0.3, 0, 0, 0, 0, 0.3, 0, 2, 3],
    stepValues: [0.3, 0.61, 0.82, 0.9],
    explanations: [
      "三个带位置的输入嵌入先进入第一层，当前 token 初始表示为 0.3。",
      "自注意力从另外两个 token 汇入信息，使上下文表示升到 0.61。",
      "FFN 在每个位置内重组通道，残差后当前表示更新到 0.82。",
      "同样结构再堆叠一层，顶层表示达到 0.9，并已包含更深的上下文组合。",
    ],
  },
  10098: {
    kind: "matrix",
    entityLabels: [
      "目标前缀长度", "因果自注意力", "目标 Query", "源端 Key/Value", "交叉上下文",
      "下一步表示", "推理输入来源", "源 token 数", "目标 token 数",
    ],
    sampleValues: [2, 0, 0, 0, 0, 0, 0, 3, 2],
    stepValues: [0.58, 0.62, 3, 0.79, 0.91, 1],
    explanations: [
      "因果 mask 只允许当前目标位置读取两个已生成 token，未来位置权重为 0。",
      "目标端状态投影成 Query，当前查询强度 0.62 表示正在寻找对应源信息。",
      "编码器的三个源 token 同时提供 Key 和 Value，并且在所有解码步保持可复用。",
      "Query 对源 Key 加权后汇入 Value，交叉上下文从 0 变为 0.79。",
      "FFN 与残差把上下文整理成下一步表示 0.91，供词表头预测。",
      "训练使用真实前词、推理改用自身预测；值 1 标记当前已切到推理输入。",
    ],
  },
  10099: {
    kind: "sequence",
    entityLabels: ["优化器更新次数", "平滑目标概率", "微批梯度和", "warmup 学习率"],
    sampleValues: [0, 0, 0, 0],
    stepValues: [0.9, 0.25, 1, 0.0004],
    explanations: [
      "标签平滑把正确类概率从 1 调到 0.9，少量质量分给其他类别以抑制过度自信。",
      "四个微批的梯度先累加并除以 4，当前平均梯度范数为 0.25。",
      "累计满四个微批才执行一次参数更新，所以优化器步数只增加到 1。",
      "warmup 初期把学习率逐步升到 0.0004，避免随机初始化阶段出现过大更新。",
    ],
  },
  10100: {
    kind: "sequence",
    entityLabels: [
      "最终序列分数", "截断后候选数", "展开候选数", "累计对数概率",
      "惩罚后分数", "保留 beam 数", "完成序列数", "上一轮 beam 数",
    ],
    sampleValues: [0, 0, 0, 0, 0, 0, 0, 2],
    stepValues: [5, 10, -1.3, -1.5, 2, 1, -0.86],
    explanations: [
      "温度缩放后只保留 top-5 token，其余候选不会进入本轮搜索。",
      "两条 beam 各展开五个 token，因此候选池暂时从 2 条变成 10 条。",
      "新 token 的 log 概率加到已有前缀，当前候选累计分数为 -1.3。",
      "重复惩罚和长度归一化把偏爱短句或复读的候选调到 -1.5。",
      "在全部十个扩展中只留下得分最高的两条，搜索宽度重新回到 2。",
      "其中一条遇到 EOS 后冻结，完成序列计数变为 1，不再继续扩展它。",
      "比较所有完成候选的归一化分数，最终选择得分 -0.86 的序列。",
    ],
  },
  10101: {
    kind: "matrix",
    entityLabels: [
      "计算预算 C", "候选参数量 N", "匹配 token 数 D", "预测损失 L", "边际收益",
      "所选候选编号", "参数项贡献", "数据项贡献", "计算约束",
    ],
    sampleValues: [0, 0, 0, 0, 0, 0, 0.5, 0, 0],
    stepValues: [5760, 12, 80, 1.7, 1],
    explanations: [
      "先固定总训练预算 C=6x12x80=5760 个相对计算单位，候选不能偷偷使用更多算力。",
      "在预算内选择约 12 个参数单位，过大模型会挤压可训练 token 数。",
      "按计算约束匹配得到 80 个 token 单位，使参数和数据都不过度短缺。",
      "把 N=12、D=80 代入两项缩放式，预测验证损失约为 1.7。",
      "第二个候选只再降低 0.06，却需要明显更多资源，因此当前选择候选 1。",
    ],
  },
} satisfies AiSceneProfileTable;
